/// <reference types="rx/ts/core/observer-lite" />
declare module Rx {
    module internals {
        interface ScheduledObserver<T> extends Observer<T> {
            ensureActive(): void;
        }
    }
}
